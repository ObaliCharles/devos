import { DetailSkeleton } from "@/components/status-screens";

/** A brief is header, outcome, rubric and reference — a detail shape. */
export default function Loading() {
  return <DetailSkeleton />;
}
